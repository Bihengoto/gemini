/*
Day 3: Operators & Control Flow - Notes

OPERATORS IN DART:

1. ARITHMETIC OPERATORS:
   + (Addition)
   - (Subtraction)
   * (Multiplication)
   / (Division - returns double)
   ~/ (Integer Division)
   % (Modulo/Remainder)
   
2. EQUALITY OPERATORS:
   == (Equal to)
   != (Not equal to)
   
3. RELATIONAL OPERATORS:
   > (Greater than)
   < (Less than)
   >= (Greater than or equal to)
   <= (Less than or equal to)
   
4. LOGICAL OPERATORS:
   && (Logical AND)
   || (Logical OR)
   ! (Logical NOT)
   
5. ASSIGNMENT OPERATORS:
   = (Simple assignment)
   += (Add and assign)
   -= (Subtract and assign)
   *= (Multiply and assign)
   /= (Divide and assign)
   
6. INCREMENT/DECREMENT:
   ++ (Increment)
   -- (Decrement)
   
CONTROL FLOW STATEMENTS:

1. IF STATEMENT:
   if (condition) {
     // code block
   }
   
2. IF-ELSE STATEMENT:
   if (condition) {
     // code block
   } else {
     // code block
   }
   
3. IF-ELSE IF-ELSE:
   if (condition1) {
     // code block
   } else if (condition2) {
     // code block
   } else {
     // code block
   }
   
4. TERNARY OPERATOR:
   condition ? expression1 : expression2
   
   Example: int result = (a > b) ? a : b;
*/

void main() {
  print("Day 3: Operators & Control Flow - Study Notes");
  print("Check other files in this folder for practical examples!");
}
